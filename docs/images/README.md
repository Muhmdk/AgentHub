# Documentation images

These screenshots are captured from the deterministic local demo using the repository's
real FastAPI routes and seeded PostgreSQL state. They are documentation evidence, not
design mockups. Regenerate them after material console changes and verify that no secret,
personal, or production data is present before committing.
